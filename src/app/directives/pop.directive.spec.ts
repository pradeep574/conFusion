import { PopDirective } from './pop.directive';

describe('PopDirective', () => {
  it('should create an instance', () => {
    const directive = new PopDirective();
    expect(directive).toBeTruthy();
  });
});
